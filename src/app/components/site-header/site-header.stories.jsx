import React from 'react';
import { storiesOf } from '@storybook/react';
import { SiteHeader } from './site-header';

storiesOf('SiteHeader', module)
  .add('Site Header', () => <SiteHeader actions={{}}/>);
